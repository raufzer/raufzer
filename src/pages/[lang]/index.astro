---
// Non-default locales only. English lives at the root, see src/pages/index.astro.
export const getStaticPaths = () => {
  return [{ params: { lang: "fr" } }];
};

import Portfolio from "@/src/components/Portfolio.astro";

const { lang } = Astro.params;
---

<Portfolio locale={lang!} />
